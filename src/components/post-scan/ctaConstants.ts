/**
 * Shared CTA labels for the post-scan / full-report flow.
 *
 * The in-page authoritative primary CTA (TopRisksCTAStrip) and the
 * sticky bottom mirror (StickyCTAFooter) MUST use this exact wording so
 * users perceive a single commercial action, not multiple offers.
 */
export const CTA_LABEL = "Get a Better Quote";
