

## Plan: Simplify ProcessSteps to 4 Steps

### Change
Edit `src/components/ProcessSteps.tsx`:
1. Remove the first item in the `steps` array ("Answer 4 quick questions")
2. Renumber remaining steps: 01 (Upload your quote), 02 (AI scans every line), 03 (Your grade is calculated), 04 (You decide what to do)

### Files
- **Edit** `src/components/ProcessSteps.tsx` — remove first step, renumber remaining four

