

## Plan: Rename Button Text in OrangeScanner.tsx

Change two instances of `"Start Compliance Audit"` to `"Run Demo Audit"` in `src/components/OrangeScanner.tsx` (lines 510 and 747). No other changes.

