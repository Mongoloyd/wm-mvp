

# Plan: Swap Phone Image for Hidden Fees Image in TruthGateFlow

## Changes

1. Copy `user-uploads://hidden_fees_below_truth_gate.avif` to `src/assets/hidden_fees_below_truth_gate.avif`
2. Update the import in `src/components/TruthGateFlow.tsx` from `cellphone.png` to `hidden_fees_below_truth_gate.avif`

No other changes — same placement, sizing, and animation.

